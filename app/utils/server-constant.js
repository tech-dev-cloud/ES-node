const EMAIL_VERIFICATION = {
  verified: 1,
  notVerified: 0,
};

const OTP_TYPE = {
  login: 'login',
};

const PRODUCT_TYPE = {
  notes: '1',
  quiz: '2',
  bulk: '3',
  course: '4',
  test_series: '5',
};

const SOLR_CORES = {
  users: 'users',
  products: 'products',
};

const GENDER = {
  male: 'male',
  female: 'female',
  other: 'other',
};

module.exports = {
  EMAIL_VERIFICATION,
  OTP_TYPE,
  PRODUCT_TYPE,
  SOLR_CORES,
  GENDER,
};
